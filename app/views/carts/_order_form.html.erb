<%= form_for order_detail, url: cart_detail_path(id: order_detail.id) do |f| %>
  <%= f.number_field :quantity, step: 1, min: 1, class: 'number-field' %>
  <%= f.submit 'update',  class: 'btn btn-warning btn-sm' %>
<% end %>
